{
  "dependencies": {
    "express": "^4.18.2",
    "idb-keyval": "^6.2.1",
    "multer": "^1.4.5-lts.1"
  },
  "scripts": {
    "serve": "npm install && npm install -g nodemon && nodemon server.js"
  }
}
